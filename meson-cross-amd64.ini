[constants]
arch = 'x86_64'

[binaries]
c = arch + '-oe-linux-gcc'
cpp = arch + '-oe-linux-g++'
ar = arch + '-oe-linux-gcc-ar'
strip = arch + '-oe-linux-strip'
pkgconfig = 'pkg-config'

[host_machine]
system = 'linux'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'
